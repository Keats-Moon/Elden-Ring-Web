# ─────────────────────────────────────────────────────────────
# 复制这个文件为 .env.local，然后填入真实值。
# .env.local 已被 .gitignore 忽略，绝不会被提交。
# ─────────────────────────────────────────────────────────────

# 【必填】Steam Web API Key
# 申请地址：https://steamcommunity.com/dev/apikey
# Domain Name 随便填（本地开发填 localhost 即可），1 分钟拿到 32 位十六进制字符串。
# ⚠️ 这个 Key 等同于你的账号权限，只放在服务端，绝不提交、绝不放前端。
STEAM_API_KEY=

# 【强烈建议填写】登录 Cookie 的签名密钥
# 任意 32 位以上随机字符串。生成方法（PowerShell）：
#   -join ((1..48) | ForEach-Object { '{0:x}' -f (Get-Random -Max 16) })
# 不填会退化为从 STEAM_API_KEY 派生，并打印警告。
SESSION_SECRET=

# 【可选】本站对外地址，用于拼装 Steam 登录回调 URL。
# 不填则自动从请求头 Host / X-Forwarded-Host 推断。
# 反向代理或自定义端口时建议显式设置，例如：
#   APP_ORIGIN=http://localhost:3000
#   APP_ORIGIN=https://my-elden-tracker.example.com
APP_ORIGIN=
